import {
  structureHour
} from '../../../helpers/structure-hour';
import { module, test } from 'qunit';

module('StructureHourHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = structureHour(42);
  assert.ok(result);
});
