<div class="toolbar">
  <a href='#' {{action 'addArea'}}>Add Area</a>
</div>

{{map-svg model=model editingState=editingState selectedArea=selectedArea}}

<div>
  {{editingState}}
</div>

{{input value=selectedArea.name}}
